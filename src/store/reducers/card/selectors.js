export const getCvc = (({CARD}) => CARD.cvc);
export const getCardName = (({CARD}) => CARD.cardName);
export const getCardNumber = (({CARD}) => CARD.cardNumber);
export const getExpiryDate = (({CARD}) => CARD.expiryDate);